#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cin >> n;
	
	vector<int> arr(n);
	
	for(int i = 0; i<n; i++){
	    cin >> arr[i];
	    
	   
	   }
	   sort(arr.begin(), arr.end());
	   
	   for(int i = 0; i<n; i++);
	   
	float median = 0;
	if(n % 2 != 0){
	median = arr[n/2];
	}
	else {
	    median = (float)(arr[n/2 - 1] + arr[n/2]) / 2;
	}
	cout << median << " ";
}
